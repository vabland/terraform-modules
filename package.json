{
  "scripts": {
    "test": "terraform -chdir=terraform fmt -recursive -diff -check",
    "fix": "terraform -chdir=terraform fmt -recursive",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.0"
  }
}
