{
  "name": "terraform-template",
  "scripts": {
    "terraform-check": "terraform -chdir=terraform fmt -recursive -diff -check",
    "terraform-fix": "terraform -chdir=terraform fmt -recursive",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.0"
  }
}
